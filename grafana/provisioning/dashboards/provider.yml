apiVersion: 1

providers:
  - name: RabbitOS
    orgId: 1
    folder: RabbitOS
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/dashboards
